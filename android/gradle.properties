org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError -Dkotlin.incremental=false -Dkotlin.daemon.jvmargs="-Xmx1G -Dkotlin.incremental=false"
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# AGP 9.0.1 recommends max SDK 36; suppress warning since we need 37
android.suppressUnsupportedCompileSdk=37
# Keep original resource names: alarm sounds are referenced via
# 'android.resource://.../raw/<name>' runtime strings (getIdentifier),
# which resource-name obfuscation would silently break.
android.enableResourceOptimizations=false
kotlin.incremental=false
